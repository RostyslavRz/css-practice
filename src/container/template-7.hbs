<div class="hdr">
  <div class="hd_up">
    <div class="lg">LG</div>
    <div class="NV">NV</div>
  </div>
  <div class="hd_lw">2</div>
</div>

{{!-- <div class="header">
  {{#with header}}
  <div class="logo">
    <span>{{brandName}}</span>
  </div>

  <nav class="header_nav">
    {{#each links}}
    <div class="nav_line">
      <a href="url" class="nav_link">{{text}}</a>
    </div>
    {{/each}}
  </nav>
  {{/with}}
</div> --}}

<main class="hero">
  {{#with hero}}
  <div class="hero_title">{{title}}</div>
  <p class="hero_descrip">{{description}}</p>
  <a href="{{button.url}}" class="button">{{button.text}}</a>
  {{/with}}
</main>

<div class="about">
  <strong class="about_title">{{about.heading}}</strong>
  <p class="about_text">{{about.info}}</p>
</div>

<div class="last_block">
  <div class="block">
    <div class="block_title">Our Articles</div>

    <div class="view">View All</div>
  </div>

  <div class="articles">
    {{#each articles}}
    {{>cardDark data=this}}
    {{/each}}
  </div>
</div>






<div class="footer">
  {{#with footer}}
  {{#each pages}}
  <div>
    <a href="{{path}}" class="nav_link">{{name}}</a>
  </div>
  {{/each}}

  <div>
    <a class="footer_copyright">{{copyright}}</a>
  </div>
  {{/with}}
</div>